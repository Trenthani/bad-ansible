---
#Openstack lab environment
cloud_infra_vars_os_servers:
  - name: app1
    metadata: group=apps, deployment_name=QA
    flavor: m2.small
    keypair: "{{ os_keypair }}"
    image: "{{ os_image }}"
    networks:
      public: 10.10.10.0/24
      private: 20.20.20.0/24
    sg: sg_app

  - name: app2
    metadata: group=apps, deployment_name=QA
    flavor: m2.small
    keypair: "{{ os_keypair }}"
    image: "{{ os_image }}"
    networks:
      public: 10.10.10.0/24
      private: 20.20.20.0/24
    sg: sg_app
    
  - name: db
    metadata: group=appdbs, deployment_name=QA
    flavor: m2.small
    keypair: "{{ os_keypair }}"
    image: "{{ os_image }}"
    networks:
      public: 10.10.10.0/24
      private: 20.20.20.0/24
    sg: sg_db

  - name: frontend
    metadata: group=frontends, deployment_name=QA
    flavor: m2.small
    keypair: "{{ os_keypair }}"
    image: "{{ os_image }}"
    networks:
      public: 10.10.10.0/24
      private: 20.20.20.0/24
    sg: sg_frtd